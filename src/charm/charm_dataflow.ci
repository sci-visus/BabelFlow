module charm_dataflow {

  namespace DataFlow {
    namespace charm {
  
      template<class TaskGraphClass, class CallbackClass>
      array [1D] CharmTask {
        entry CharmTask(std::string config);
        entry void addInput(DataFlow::TaskId source, std::vector<char> input);
      };

      // Note: temporarely here for testing, but should not
      array [1D] CharmTask<Reduction,ReductionCallbacks>;

    }          
  }
};
