mainmodule reduction {

  extern module charm_dataflow;

  readonly CProxy_Main mainProxy;
 
   mainchare Main {
      entry Main(CkArgMsg* m);
      entry void done();
  };          
  
  namespace DataFlow {
    namespace charm {
      array [1D] CharmTask<Reduction,ReductionCallbacks>;
    }
  }
};
